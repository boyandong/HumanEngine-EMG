# HumanEngine scientific-kernel handover

## CURRENT HANDOVER — continuation-identity closure, 2026-09-20

Status: **READY FOR FINAL ACCEPTANCE AUDIT**. This section supersedes the
historical handovers below. It does not authorize real teacher validation/training.

### Objective
Close fresh-audit findings X01–X04 as one scientific-continuation identity class:
optimizer implementation/options, named state ownership, parameter topology and
successful diagnostic observational purity. Implementation, bounded adjacent review,
synthetic CPU verification and evidence are complete. Start with
`CONTINUATION_CLOSURE_REPORT.md`, `CONTINUATION_DECISIONS.md` and
`CONTINUATION_EVIDENCE.json`.

### Frozen decisions
A→F→U, raw E, S256/R128, detached-R APIs, teacher three-scales/two-layers,
residual/family/CAGrad/VICReg/geometry mathematics, burn-in, quota, effective rank
and M_A=.05*L_phys are unchanged. v0.1 is single-process, eager CPU correctness,
single-threaded update, immutable Python, supported nn.Module/optimizers only. No
compile/JIT/distributed/multiprocessing/native mutation/dispatch disabling guarantee.
Checkpoint/scientific state is v4; v1/v2/v3 fail loudly without reviewed migration.

### Verified state
Final local suite: **191 passed, 0 failed/errors/skipped, 85.21 seconds**. All 169
previous cases remain green; 21 continuation tests plus one expanded legacy-schema
case pass. All 12 unchanged independent scientific-oracle groups pass. Continuous
five-step Adam execution exactly equals two steps + checkpoint/resume + three steps
for named model state, EMA/step and optimizer state by scientific name.

X01: optimizer class/source, PyTorch version and actual param-group update options
are bound. X02: state is serialized/loaded by qualified parameter name with exact
ordered group membership; class/settings/order/missing/extra/wrong-group cases reject.
X03: tied/shared/overlapping parameter storage is unsupported and rejected; successful
loads receive exact post-load named state verification. X04: diagnostics execute in
ReadOnlyEvaluation plus a transaction that restores registered state and RNG even on
success; pure diagnostics work and mutation attempts cannot alter continuation.

### Scientific evidence
Before repair, current external reproductions confirmed Adam→AdamW next-step delta
difference .0099980002, swapped same-shape LSTM bias state difference .0118801538,
aliased-bias loaded error .1685013771 / forward difference .0782297850, and diagnostic
eps/buffer changes affecting later derivatives. New oracles compare actual named
tensors/state and next updates, not only identity metadata. Evidence remains synthetic.

### Unknown / contaminated state
Fresh final acceptance is still required. Real teacher quality, information
preservation, transfer, real manifests/statistics/thresholds, GPU/AMP and deployment
performance remain Unknown. Hashes establish consistency, not measurement truth or
authorship. No arbitrary-Python sandbox is claimed. Historical S6 remains 308 match,
12 changed, 1 missing; attribution/authorization UNKNOWN and no dependency on this
synthetic kernel was demonstrated.

### External runtime state — MUST reverify in next session
Only local CPU Python/pytest/hash processes ran and completed with known outcomes.
No GPU, remote, service or real training job was launched. Verified environment:
`D:/Anaconda3/envs/emgforce/python.exe`, Python 3.11.16, torch 2.4.1+cpu. Other-lane
runtime state is UNKNOWN; reverify process/job ownership before later work.

### Worktree state
Project root remains outside Git. Nested `baseline/emg2pose` must be rechecked by the
final auditor; its last verified HEAD was `5f6f62b1a0a08426adffe55900842e75a8adb38c`.
All intentional edits/artifacts are under `humanengine/`. Use continuation before/after
inventories and evidence hashes rather than inventing project-wide Git provenance.

### Next actions
Run one fresh, bounded independent acceptance audit named
**HUMANENGINE-FINAL-ACCEPTANCE-AUDIT-V01**. Reproduce X01–X04, run the positive
continuation oracle, negative matrix and preserved contracts, then stop. If they pass,
the infrastructure boundary is accepted; do not invent another open-ended cycle.

### Do NOT
No real teacher/HE_FULL training, GPU/remote jobs, sweeps, architecture redesign,
S6 modification/rebaseline, hidden-answer inspection, schema-v3 silent migration,
or representation-performance claim. Out-of-boundary compile/JIT/distributed/native/
malicious-code questions are future scope, not v0.1 blockers.

### Recommended new session
**HUMANENGINE-FINAL-ACCEPTANCE-AUDIT-V01**. This remediation-to-final-audit phase
change is the Session Hygiene handoff point; retire this workbench after delivery.

---

## CURRENT HANDOVER — autonomous execution-integrity remediation, 2026-09-19

Status: **READY FOR FRESH INDEPENDENT RE-AUDIT**. This section supersedes the
historical handovers below. It does not authorize real teacher validation/training.

### Objective
Close the latest audit's R01–R06/N01 execution-integrity failures without changing
the scientific architecture. Investigation, repair, synthetic verification and
evidence/reporting are complete for fresh independent challenge. Start with
EXECUTION_REMEDIATION_REPORT.md (18 required sections), EXECUTION_DECISIONS.md,
EXECUTION_EVIDENCE.json and EXECUTION_PRESERVED_CONTRACTS.json.

### Frozen decisions
A→F→U; raw E; S256/R128; detached-R APIs; independent three-scale/two-layer teacher;
residual/family/CAGrad/VICReg/geometry mathematics unchanged. M_A private coefficient
.05, no family alpha. Burn-in, quota and effective-rank code unchanged this task.
Only humanengine/ may be modified. No real teacher/HE training, GPU/remote jobs,
sweeps, S6 edits, hidden-answer inspection or performance claims.

Checkpoint/scientific-state/teacher certificates are now v3. v1/v2 checkpoints
fail loudly; no silent migration. Use externally pinned expected_identity when
selecting a particular scientific checkpoint. Hashes are consistency identities,
not measurement-truth or authorship attestations.

### Verified state
Final synthetic CPU suite: **169 passed, 0 failed/errors/skipped, 86.036 seconds**;
96 previous + 73 new. Final JUnit: execution-final-suite.xml. Unchanged independent
oracles: all12 preserved-contract groups passed; source hashes unchanged. This
re-execution was owned by remediation and is not a fresh audit verdict.

Transient .data writes rejected before execution. Actual eps/stride protected.
Prebuilt graphs rejected. Persistent/nonpersistent buffers, tensor binding/layout,
hooks and RNN caches covered. Sampler corruption no longer invokes a contaminated
rollback loader. Late HE/teacher failures restore existing gradients. An injected
rollback failure poisons continuation and subsequent operation fails closed.
Direct FrozenTeacher rejects all17 tested contradictory protocol/header fields.
Nested checkpoint/statistics identities reject outer-only target substitutions;
valid nonzero moments resume. Duplicate-buffer and mode/trainability consistency
are checked. Sources/configs/test/report artifacts have recorded content hashes.

### Scientific evidence
Analytical g_reserve=8*p matches authoritative toy state; the prior wrong-state
case is rejected before its forward. Direct frontend output changes establish
that eps/stride are behavior-relevant. Independent exact state/RNG comparison
checks rollback. Preserved checks cover causality/streaming, detached-R/shadow,
teacher six targets, clean/masked isolation, L_R/physics, E0, burn-in, unique grants,
effective rank, normal/extreme CAGrad and M_A. All evidence is synthetic; real
teacher utility and HumanEngine representation usefulness remain Unknown.

### Unknown / contaminated state
Fresh independent acceptance remains required. Prior self-test success did not
close all audit attacks; do not replace independent review with test counts.
The eager guard is not an arbitrary-Python sandbox; register all participating
modules, construct loss graphs inside closures, pass accessed sampler. Factory
supports evaluation_modules=(teacher,...) in addition to model/heads. Unsupported:
deliberate dispatch bypass, foreign-native writes, hotpatches, concurrent callbacks,
compiled/JIT/distributed execution; GPU behavior unverified. External objects and
retained graphs are not rollback state. See report limitations before extension.

Protected entry-to-exit:321/321 unchanged. Historical baseline:308 match,12 changed,
1 missing, all differences present at entry. Ownership/authorization unresolved;
do not infer blame, rebaseline or alter S6. No demonstrated synthetic-kernel
dependency on those external records. Future real data manifests need fresh audit.

### External runtime state — MUST reverify in next session
Only local CPU synthetic verification/hash processes were launched here; all
completed with known outcomes. No GPU, remote, service or real training job was
launched. Python: D:/Anaconda3/envs/emgforce/python.exe; torch2.4.1+cpu; exact
versions are in evidence. Other-lane runtime state is UNKNOWN; do not stop or adopt
processes from this narrative. Reverify host/PID/purpose/config/data/log/checkpoint
ownership before any later authorized execution. No host crash or unknown-outcome
incident occurred in this remediation; known test failures were inspected/repaired.

### Worktree state
Root outside Git. Nested baseline/emg2pose clean at
5f6f62b1a0a08426adffe55900842e75a8adb38c. No Git init/commit/branch/worktree created.
Every task edit/artifact is under humanengine/. FROZEN_BASELINE and historical
reports/evidence/JUnit retained. Use EXECUTION_LANE_BEFORE/AFTER, protected before/
after, and EXECUTION_EVIDENCE hashes; reverify current source before trusting them.

### Next actions
Open a fresh independent read-only audit. Reread governance, this current handover,
report/decisions, original specs, both independent audits and actual source/tests.
Check hashes and external runtime anew. Attack transient wrong-state computation,
actual executable constants, all-buffer coverage, fresh-graph provenance,
rollback/poison behavior, target substitution, failed resume and direct certificate
contradictions; rerun preserved scientific contracts. Do not modify independent
audit records. Only fresh independent acceptance may authorize the next phase.

### Do NOT
No teacher validation/pretraining, HE_FULL, GPU/remote jobs, sweeps, architecture
redesign, hidden labels, S6 editing, rebaselining, authorship inference, tolerances
weakened to obtain a pass, or clearing poison flags to continue a failed rollback.
Do not present synthetic health fixtures as measured physiological usefulness.

### Recommended new session
**HUMANENGINE-REAUDIT-FRESH-EXECUTION-INTEGRITY**.
Session Hygiene Protocol: this is a completed remediation-to-independent-audit
phase transition. Retire this workbench; preserve it as remediation history. The
repository handover/evidence, not this long conversation, is the next task's truth.

---

## Historical kernel-remediation handover (superseded), 2026-09-19

This section supersedes the historical implementation handover retained below.

### Objective
F01–F09 kernel remediation complete for independent re-audit. Read
REMEDIATION_REPORT.md (17 sections), REMEDIATION_EVIDENCE.json, and
F06_PROVENANCE_RECONCILIATION.md. Status: READY FOR INDEPENDENT RE-AUDIT,
never authorization for real teacher validation/training.

### Frozen decisions
Architecture and api-detached-r-v1 route unchanged. Core A→F→U, raw E, dual S/R,
independent teacher and residual/family/CAGrad formulas retained. M_A private
coefficient .05, no family alpha. Only humanengine/ modified; original specs and
all other lanes remain protected. v2 checkpoint, teacher certificate, quota identity,
and singular-value effective-rank definitions are intentional version changes.

### Verified state / Scientific evidence
Audit counterexamples reproduced before edits. Final CPU suite: 96 passed,
0 failed/errors/skipped, 13.113 seconds; old59 + new37. JUnit and source hashes
in remediation evidence. No real training, evaluation, remote/GPU jobs, sweeps,
environment installation, hidden-label inspection, or S6 endpoint computation.
321 registered external entries unchanged from remediation entry to final.

### Unknown / contaminated state
Historical frozen baseline now312 match,8 changed,1 missing; all nine predate
this remediation's source edits. Original audit saw eight; extra staged-site
manifest drift already existed at entry. All nine exact authorization/authorship
chains UNRESOLVED; do not infer blame or overwrite baseline. Synthetic health
fixtures are not real quality evidence. Re-audit still required.

### External runtime state — MUST reverify
Only local CPU pytest/hash processes were launched and completed. No remote,
GPU, service, or training job was started by this remediation. Other-lane runtime
state UNKNOWN; do not stop or adopt processes based on this handover. Verify
host/PID/purpose/config/data/log/checkpoint ownership before later authorized work.

### Worktree state
Root outside Git; nested baseline clean at5f6f62b1a0a08426adffe55900842e75a8adb38c.
No Git initialization, commits, branches or worktrees. All edits/artifacts inside
humanengine/. FROZEN_BASELINE and old test-results retained. Use content hashes.

### Next actions
Fresh independent read-only re-audit. Attack content/semantic checkpoint binding,
teacher certificate, actual closure mutation rollback, unique grants and burn-in.
Macrostep standalone callers must register stateful_modules and pass sampler when
closures can access them. Checkpoint v1 is rejected; no silent migration. Verify
F06 separately with authorized exact change records without reading hidden answers.

### Do NOT
No teacher pretraining, HE_FULL, GPU/remote jobs, sweeps, architecture redesign,
S6 modifications, hidden labels, or self-authorization for scientific validation.
Do not equate96 tests or certificates with measured physiological usefulness.

### Recommended new session
HUMANENGINE-REAUDIT-20260919-KERNEL-REMEDIATION.
Phase boundary: retire this implementation/remediation workbench and use a fresh
independent auditor. No host crash or unknown mutation incident occurred here.

---

## Historical implementation handover (superseded; provenance retained)

Date: 2026-09-19. Status: implemented and locally tested; no scientific training.

## Objective

Implement the approved scientific kernel faithfully in a new lane, verify causal,
gradient, teacher, family-budget and provenance contracts, and stop before any real
teacher/HE training. The complete user task is SPEC_IMPLEMENTATION_TASK.md.

## Frozen decisions

- Shared causal F; A→F→U; raw amplitude E before A; S256/R128; H=[S,R].
- All explicit APIs read [S,stopgrad(R)]. Pose geometry is the separate G(S) route.
- Independent EMG-only student/EMA preparation; frozen three-scale/two-layer
  teacher targets; residual masked + .1 observed + .05 physical + .01 variance.
- Family budgets 0.8/K plus reserve .2, no dataset-name votes; fixed reference
  scales; one full-core CAGrad solve; c=0 at K=1, default .25 for K>=2.
- Private-head update rules, replay/output-distillation contracts, no H distillation.
- Baseline, checkpoints, canonical16, DB9 and S3–S6 remain frozen; S6 stays blinded.

## Verified state

The final local CPU suite has 59 passing tests, zero failures/skips, recorded in
test-results.xml and IMPLEMENTATION_REPORT.md. It includes every high-priority
contract from the request plus an end-to-end synthetic microstep. Production
dimensions/routing are retained; the integration fixture explicitly reduces
support thresholds for wiring tests and is not scientific FULL evidence.

Environment: D:\Anaconda3\envs\emgforce\python.exe; torch 2.4.1+cpu,
NumPy 1.26.4, SciPy 1.14.1, pytest 8.4.1. The default D:\Anaconda3\python.exe
could not be launched under the sandbox; an already-existing CPU environment was
used with approved execution. No installation, wrapper process or environment
repair was attempted. Those were known access failures, not unknown outcomes.

FROZEN_BASELINE.json contains 321 hash records: the prior 309 protected artifacts,
five official checkpoints, six design records and root AGENTS. Final integrity
results and created-file hashes are in IMPLEMENTATION_EVIDENCE.json. The original
design documents, root governance and nested baseline remain unchanged.

## Scientific evidence

Only implementation correctness evidence exists: deterministic synthetic tests,
analytical/numerical CAGrad comparisons, gradient reachability, causal perturbation,
canonical parity and frozen hashes. Literature principles come from the approved
design's recorded review; no new external survey or architecture selection occurred.
The CAGrad implementation follows addendum 2.3's dual and checks a primal/dual
certificate. No paper or synthetic test score is presented as a HE performance claim.

## Unknown / contaminated state

No real teacher passed health gates, no HumanEngine was trained scientifically,
and no force/contact/fatigue/generalization/latency benefit was measured. Synthetic
teacher exports are explicitly marked as synthetic evidence. Real-data manifests,
noise estimates, fixed reference/statistics artifacts, teacher budget and health
thresholds must be supplied/preregistered for a future authorized experiment.

No blocking design ambiguity was found. The implementation map distinguishes
source rules, repository facts and engineering choices. Nonfinite core gradients
abort before updates because no finite g0 fallback exists; finite solver failures
return g0 with an explicit reason. This is recorded numerical domain validation.

No hidden/evaluator labels, S6 endpoints, real training data or remote runtime were
read/executed during testing. Prior scientific-lane exposure remains governed by
those lanes' records and is not reset by this implementation.

## External runtime state — MUST reverify

No remote/GPU/training/service job was launched. Local pytest processes completed.
Other lanes' external jobs were not inspected and remain UNKNOWN. Before any new
authorized execution reverify host, process/job ID, ownership, data/config/source
hashes, log/checkpoint paths and stop/resume procedure; do not trust old descriptions.

## Worktree state

All additions are under humanengine/. No pre-existing project file was modified.
Project root remains outside Git; nested baseline/emg2pose is untouched. No Git
initialization, branch, commit, worktree or source-control restructuring occurred.
Use IMPLEMENTATION_EVIDENCE.json for source provenance, not a claimed root commit.

## Next actions

1. Review IMPLEMENTATION_REPORT.md, especially exact routing, source-defined rules
   versus implementation choices, and remaining empirical limitations.
2. The next safe local verification command is in report Q. It is deliberately
   not launched automatically after delivery.
3. A subsequent validation session may review/extend correctness tests as requested.
   Keep the existing assertions and source precedence; any alternative route or
   backbone requires a distinct scientific proposal, not a hidden implementation fix.
4. Only after explicit new authorization, prepare real training manifests,
   measurement-noise/statistics/reference artifacts, EMG-only teacher health
   preregistration and compute budget. Do not start a scientific run from test fixtures.

## Do NOT

- No real teacher pretraining, HE_FULL, GPU jobs, remote jobs or parameter sweeps.
- No frozen artifact edits, hidden label/evaluator reads or S6 endpoint computation.
- No mixed old future→R gradient with the new detached-R specification.
- No per-window normalization that removes physiological amplitude.
- No missing-label zeros as valid observations; no incomplete-family update.
- No HE EMA masquerading as an independent teacher; no pose-based teacher selection.
- No per-block CAGrad or private-head inclusion; no unlogged after-solver SSL update.
- No interpretation of high rank/reconstruction as validated physiology.

## Recommended new session

`HUMANENGINE-VALIDATION-20260919-SCIENTIFIC-KERNEL`

Use the actual start date. Implementation→independent validation/experiment
preparation is a phase boundary; use a fresh session and retain this one as the
implementation record. Any actual incident requires an INCIDENT-named retired
forensic task and a successor that reconciles external state before proceeding.
