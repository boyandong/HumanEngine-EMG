# Scientific kernel implementation lane

Read the project-root AGENTS.md and this lane's SESSION_HANDOVER.md before work.
Repository records are the source of truth; sessions are temporary workbenches.

The user's 2026-09-19 implementation request authorizes this lane's kernel code
and local CPU scientific-correctness tests. It supersedes the historical
design-only stop condition, but does not authorize real teacher preparation,
HE_FULL training, GPU/remote jobs, sweeps, or scientific performance claims.

Precedence: SPEC_IMPLEMENTATION_TASK.md; focused addendum for multi-API budgets,
residual targets and API-to-R routing; original architecture otherwise. Keep all
frozen baseline/canonical/DB9/S3–S6 artifacts unchanged and S6 blinded. Never
weaken tests to preserve an implementation that violates the scientific contract.

Apply the root Session Hygiene Protocol proactively, including context-size
warnings around the user's 500K/800K heuristics (not cumulative token accounting),
two consecutive unknown/interrupted outcomes, actual host/tool crash recovery,
repeated workarounds, phase changes and complex external execution. Retire actual
incident sessions as forensic records. Before handoff record Objective, Frozen
decisions, Verified state, Scientific evidence, Unknown/contaminated state,
External runtime state to reverify, Worktree state, Next actions, Do NOT and a
recommended <PROJECT>-<PHASE>-<YYYYMMDD>-<PURPOSE> name.

Current authorized task stops after implementation, local tests and final report.
Do not treat an available verification command or an unbound config as training
authorization. Tests with synthetic teacher health/identity statistics are wiring
fixtures only. Preserve that labeling in any downstream artifact.
