# Remediation decisions (2026-09-19)

The independent audit supersedes the earlier 59-test confidence. Before source
edits, its F01–F05/F07/F08 constructions were executed again; results are in
REMEDIATION_REPRODUCTIONS_BEFORE.json. F08's old PASS only meant finite update,
not finite diagnostics. Protected entry state is recorded separately, without
rewriting FROZEN_BASELINE.json. Current drift is nine paths, including a newly
observed staged_site_manifest.json difference; attribution remains unknown.

Old test correction, recorded BEFORE editing the test: the quota test expected a
single window's five current/two future anchors to pay seven/three by replacement.
This contradicts unique valid exposure; the correct result is NA with deficits
two/one. Coverage v2 must supply explicit indices, and consumers apply those exact
indices, never the first N valid positions again. Existing valid assertions stay.

Checkpoint v2 rejects semantic metadata mismatches before loading. Statistics are
expected frozen artifacts, so the runtime must already supply identical content
and definitions. Parameter values (including A/U) are restored from a content-bound
payload; adapter identity and trainability mode must match. v1 is not silently
migrated. Health certificate v2 binds the evaluated artifact, not invented quality
thresholds. Synthetic fixtures remain synthetic.

M_A v0.1 private objective is physical_weight * L_phys (default .05), with no
reserve_alpha multiplier. Effective rank v2 uses singular-value probabilities;
covariance spectrum remains a separate diagnostic. No thresholds are changed.
